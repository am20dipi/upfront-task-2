class CategoryApi {
    /* static url = `${baseUrl}/categories`

    static fetchCategories() {
        fetch(this.url)
        .then(resp => resp.json())
        .then(json => json.forEach(catObj => {
            let category = Category.findById(catObj.id)
            category.addToDropDown()
            category.render()
        }))
        .catch(this.handleError)
    } */

}